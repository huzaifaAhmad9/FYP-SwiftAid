import 'package:flutter/material.dart';

class CreateShifts extends StatefulWidget {
  const CreateShifts({super.key});

  @override
  State<CreateShifts> createState() => _CreateShiftsState();
}

class _CreateShiftsState extends State<CreateShifts> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Implementing'),
      ),
    );
  }
}
